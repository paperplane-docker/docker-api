kind: pipeline
name: default

workspace:
  path: /source

steps:
  - name: build
    image: plugins/docker
    settings:
      username: p01
      password:
        from_secret: P01_DOCKER_PASSWORD
      repo: docker.p01.cc/p01/socat
      tags:
        - latest
      registry: docker.p01.cc

trigger:
  branch:
    - master
